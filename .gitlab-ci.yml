stages:          # List of stages for jobs, and their order of execution
  - Build

before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - ssh-add ~/.ssh/id_rsa
  - ssh-keyscan -H 'gitlab.com' >> ~/.ssh/known_hosts

Deploy Changed to Server:       # This job runs in the build stage, which runs first.
  stage: Build 
  script:
<<<<<<< HEAD
    - git diff --name-only --diff-filter=d HEAD~1 # exclude delete
	  - scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -r ./src/** $SSH_USER@$DEV_HOST:/home/ubuntu/app/
  rules:
    - if: "$CI_COMMIT_BRANCH == 'dev'"
=======
    - 'git diff --name-only --diff-filter=d HEAD~1' # exclude delete
	- scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -r ./src/** $SSH_USER@$DEV_HOST:/home/ubuntu/app/
  rules:
    - if: "$CI_COMMIT_BRANCH == 'dev'"
>>>>>>> 5b32f0dd8bbd54956d6f3ad19303ff31d834cdd9
